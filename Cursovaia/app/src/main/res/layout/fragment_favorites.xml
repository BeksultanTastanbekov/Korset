<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorSurface"
    tools:context=".ui.favorites.FavoritesFragment">

    <!-- TabLayout для вкладок "Избранное" и "Коллекции" -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_layout_favorites"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabMode="fixed"
        app:tabGravity="fill"
        app:tabIndicatorColor="?attr/colorPrimary"
        app:tabTextColor="?android:attr/textColorSecondary"
        app:tabSelectedTextColor="?attr/colorPrimary"
        android:contentDescription="Вкладки Избранное и Коллекции"/>

    <!-- ViewPager2 для отображения контента вкладок -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager_favorites"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>