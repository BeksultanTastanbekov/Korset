<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Состояние: Выбрано -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimary"/> <!-- Акцентный цвет -->
            <corners android:radius="4dp"/>
        </shape>
    </item>
    <!-- Состояние: Занято (добавим позже кастомное состояние или будем управлять программно) -->
    <!-- <item android:state_enabled="false"> ... </item> -->
    <!-- Состояние: Свободно (по умолчанию) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#CCCCCC"/> <!-- Серый цвет -->
            <corners android:radius="4dp"/>
        </shape>
    </item>
</selector>